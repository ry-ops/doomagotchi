# ESP-IDF build output
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.cache/

# editor / OS
.vscode/
.DS_Store

# assets we never commit (see CLAUDE.md constraint #2)
*.WAD
*.wad
!*.WAD.sha256
